DEFAULT_URI=http://localhost
# Nginx
NGINX_PORT=80

# Database (test)
DB_ROOT_PASSWORD=root
DB_DATABASE=symfony
DB_USER=symfony
DB_PASSWORD=symfony
DB_PORT=3306

# phpMyAdmin
PMA_PORT=8080
DATABASE_URL="mysql://symfony:symfony@database:3306/symfony_test?serverVersion=11&charset=utf8mb4"

# Mailer (null transport for tests)
MAILER_DSN=null://null
